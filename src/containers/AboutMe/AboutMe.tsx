import * as React from "react";
import styles from "../Social/Social.module.scss";

const AboutMe: React.FC = () => {
  return <section className={styles.wrapper}>aaa</section>;
};

export default AboutMe;
